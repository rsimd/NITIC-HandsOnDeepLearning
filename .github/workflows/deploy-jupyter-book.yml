# Jupyter Book 2 (MyST) を GitHub Pages に公開する
# Settings → Pages で Source を「GitHub Actions」に設定すること
name: Deploy Jupyter Book
on:
  push:
    branches: [main]

env:
  BASE_URL: /${{ github.event.repository.name }}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/configure-pages@v4

      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - uses: actions/setup-python@v5
        with:
          python-version-file: .python-version

      - name: Install dependencies
        run: uv sync --frozen --all-groups

      - name: Build Jupyter Book site
        run: uv run jupyter-book build --html --ci

      # ノート内 iframe の相対パス python_etc/*.html は、ページ URL が /…/python のとき
      # /…/python_etc/… に解決される。MyST は既定でこれらを成果物に含めないため手で同期する。
      - name: Sync python_etc HTML for iframes
        run: |
          mkdir -p _build/html/python_etc
          cp notebooks/python_etc/*.html _build/html/python_etc/

      # Jekyll が _ で始まるパスを落とすため build/_assets 等が 404 になるのを防ぐ
      - name: Add .nojekyll for GitHub Pages
        run: touch _build/html/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _build/html

      - id: deployment
        uses: actions/deploy-pages@v4
